# Dépendances requises pour exécuter le script d'analyse des ventes
# Installez-les avec la commande : pip install -r requirements.txt

pandas>=2.0.0        # Manipulation et analyse des données (DataFrames, CSV)
matplotlib>=3.7.0    # Génération des graphiques et visualisations